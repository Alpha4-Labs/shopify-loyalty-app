name = "shopify-loyalty-app"
main = "worker.js"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Environment variables (set via wrangler secret put)
# SHOPIFY_WEBHOOK_SECRET
# LOYALTEEZ_BRAND_ID (optional)
# LOYALTEEZ_API_URL (optional, defaults to testnet)

# Custom Domain Route
routes = [
	{ pattern = "shopify-demo.loyalteez.app/*", zone_name = "loyalteez.app" }
]

[env.production]
vars = { }

[env.staging]
vars = { }
